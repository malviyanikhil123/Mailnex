/**
 * Seed orchestrator.
 * Users now register dynamically via the web application (/register).
 * When a user registers, their account is auto-provisioned with the universal templates and initial settings.
 */
console.log("No static user seeding needed. Users register dynamically through the web interface.");
process.exit(0);
