# Job Application Platform

A monorepo job-application platform with a Fastify + TypeScript backend.

## Structure

- `backend/` — Fastify + TypeScript API server
- `docs/` — Specifications and design documents

## Getting Started

```bash
cd backend
cp .env.example .env
# Fill in your DATABASE_URL and secrets in .env
npm install
npm run dev
```
